
A very simple Jetty HTTP Server that only has SSL baked in.

The idea is very similar to Apache tomcat but multiple resources are not supported, only one webapp per instance.
Certbot provides the keystore which is specified via the configuration file.
